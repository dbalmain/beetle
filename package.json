{
  "name": "beetle",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Beetle — Vim Web. WASM vs TypeScript vici.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "tsc --noEmit -p packages/contract && vitest run",
    "typecheck": "tsc --noEmit -p packages/contract",
    "bench": "echo 'not yet: benches land in phase 6' >&2 && exit 1",
    "build:wasm": "echo 'not yet: wasm-pack wrapper lands in phase 2' >&2 && exit 1"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
